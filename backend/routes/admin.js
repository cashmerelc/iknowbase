const express = require('express');
const router = express.Router();

// This is a placeholder for future admin routes
router.get('/', (req, res) => {
  res.send('Admin route placeholder');
});

module.exports = router;
